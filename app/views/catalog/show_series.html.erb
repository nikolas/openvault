<h3><%= @document.title %></h3>
<div class="sep-line"></div>
<p><%= @document.summary %></p>
<div id="series_content">
  <!-- show images -->
  <% @document.images.each do |image| %>
    
  <% end %>
  <div class="sep-line"></div>
  <!-- show videos -->
  <% @document.videos.each do |video| %>
  
  <% end %>
  <div class="sep-line"></div>
  <!-- show programs -->
  <% @document.programs.each do |program| %>
    
  <% end %>
</div>