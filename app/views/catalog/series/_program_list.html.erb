<div class="programs-list">
  <h4>Programs in this series</h4>
  <% if @ov_asset.programs.empty? %>
    <p>Currently we do not have any programs information available for this series.</p>
  <% else %>
    <ul>
      <% @ov_asset.programs.each do |program| %>
        <li><%= link_to program.title, catalog_path(program.id) %></li>
      <% end %>
    </ul>
  <% end %>
</div>