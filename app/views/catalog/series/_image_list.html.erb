<% if @ov_asset.images.present? %>
  <div class="image-list">
    <h4>Images in this series</h4>
    <ul>
      <% @ov_asset.images.each do |image| %>
        <li><%= link_to image.title, "/image/#{image.id}" %></li>
      <% end %>
    </ul>
  </div>
<% end %>
